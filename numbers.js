let pi = 3.14159265359
/**
 * toString() transforma um número em string
 */
console.log(pi.toString());

/**
 * toFixed(2) para informar o número de casas decimais que o número vai ter
 */
console.log(pi.toFixed(2));
